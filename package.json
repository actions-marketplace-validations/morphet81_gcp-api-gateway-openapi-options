{
  "name": "gcp-api-gateway-openapi-options",
  "version": "0.0.1",
  "description": "Append options to open-api file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js",
    "version": "npm run build && git add dist",
    "release": "np && git tag -f v1 && git push --tags -f"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "np": "^8.0.4"
  }
}
